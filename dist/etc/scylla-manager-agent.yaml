# Scylla Manager Agent config YAML

# Bind REST API to the specified TCP address using HTTP protocol.
# http: 127.0.0.1:10001

# Override Scylla configuration options, default values are read from the Scylla
# configuration file /etc/scylla/scylla.yaml.
#scylla:
#  api_port: 10000
#  prometheus_port: 9180
