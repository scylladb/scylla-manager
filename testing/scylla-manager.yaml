http: 127.0.0.1:56080

logger:
  mode: stderr
  level: info

database:
  hosts:
    - 172.16.1.100
  keyspace_tpl_file: dist/etc/create_keyspace.cql.tpl
  migrate_dir: schema/cql
  timeout: 1s

ssh:
  user: scylla-manager
  identity_file: testing/scylla_cluster/scylla_manager.pem

repair:
  auto_schedule_start_time_margin: 1m
