use: update --cluster <id|name> [flags] [<validate_backup/task-id>]

short: Modify properties of the existing backup validation task

long: |
  This command allows you to modify properties of an already existing backup validation task.
  If there is one backup validation task the 'validate_backup/task-id' argument is not needed.
