use: history --cluster <id|name> [flags] <type/task-id>

deprecated: |
  use sctool info instead.

short: Show run history of a task

long: |
  This command shows a details about task run history for a given task.

limit: |
  Limits the number of returned results.
