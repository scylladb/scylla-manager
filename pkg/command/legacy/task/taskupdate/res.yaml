use: update --cluster <id|name> [flags] <type/task-id>

deprecated: |
  use sctool backup|repair update instead.

short: Modify a task

long: |
  This command changes generic task parameters such as schedule.
